{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions for DERIVE",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "stronghold:default",
    "log:default"
  ]
}
